# weatherapp-django
## Packages
.Requests

## setting up the project
use pipenv for virtual environment \
`pipenv install --dev`\
if virtual enviroment is already activated\
`pipenv sync`\
or\
`pipenv sync --dev`
